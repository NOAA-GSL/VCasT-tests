[pytest]
markers =
    integration: slow, subprocess-driven end-to-end tests (test_framework.py) that invoke the vcast CLI against examples/ and compare outputs to expected_outputs/ fixtures. Everything else in this repo is a fast, isolated unit test.
